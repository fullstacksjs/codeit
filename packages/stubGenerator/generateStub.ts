// TODO implement Stub generator

// import { Language } from '../core';
// import { match } from 'ts-pattern';

// const generateStub = (syntax: string, language: Language): string =>
// match(language)
// .with('TypeScript', () => typescriptGenerate(syntax))
// .with('F#', () => fsharpGenerate(syntax))
// .with('Go', () => goGenerate(syntax))
