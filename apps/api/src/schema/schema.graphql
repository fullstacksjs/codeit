### This file was generated by Nexus Schema
### Do not make changes to this file directly

enum Language {
  C
  Cpp
  Csharp
  Fsharp
  Go
  Haskell
  Java
  JavaScript
  Lua
  Php
  Python
  Ruby
  Rust
  TypeScript
}

type Player {
  displayName: String!
  email: String!
  id: ID!
  username: String!
}

type Puzzle {
  constraint: String!
  id: ID!
  initialTemplate(language: Language!): String!
  inputDescription: String!
  mode: String!
  outputDescription: String!
  testCases: [TestCase]!
  title: String!
}

enum PuzzleMode {
  normal
  reverse
}

type Query {
  getPlayerById(id: ID!): Player
  getPuzzleById(id: ID!): Puzzle
  getPuzzleByTitle(title: String!): Puzzle
  getRandomPuzzle: Puzzle
  getRandomPuzzleByMode(mode: PuzzleMode!): Puzzle
}

type TestCase {
  id: ID!
  input: String!
  mode: String!
  title: String!
}
